import { SizeLetrasDirective } from './size-letras.directive';

describe('SizeLetrasDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeLetrasDirective();
    expect(directive).toBeTruthy();
  });
});
